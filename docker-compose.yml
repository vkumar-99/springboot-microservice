services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: catalog-db
      POSTGRES_USER: catalog-user
      POSTGRES_PASSWORD: catalog-pwd
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data